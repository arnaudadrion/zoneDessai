{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <h2 class="">Nouvelle question</h2>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}
</div>
{% endblock %}

