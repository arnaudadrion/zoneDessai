<header>
    <div class="container">
        <div class="row">
            <div class="col-3">
                <h1>Test & Show</h1>
            </div>
            <div class="col-6">

            </div>
            <div class="col-3 text-right">
                {% if app.user %}
                    <p>{{ app.user.email }}</p>
                {% else %}
                    <a class="" href="{{ path('app_security_login') }}">Login</a>
                {% endif %}
            </div>
        </div>
    </div>
</header>
